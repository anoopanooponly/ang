/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ApiBuilder} from './api-builder';

describe('ApiBuilder', () => {
  it('should create an instance', () => {
    expect(new ApiBuilder()).toBeTruthy();
  });
});
